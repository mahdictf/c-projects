{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C program",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${fileDirname}",
      "environment": [],
      "console": "integratedTerminal",   // <-- Use this instead of externalConsole
      "preLaunchTask": "Build C"
    }
  ]
}
